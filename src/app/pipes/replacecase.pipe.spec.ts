import { ReplacecasePipe } from './replacecase.pipe';

describe('ReplacecasePipe', () => {
  it('create an instance', () => {
    const pipe = new ReplacecasePipe();
    expect(pipe).toBeTruthy();
  });
});
